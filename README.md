# An example C extension module for Python


Clone, build and run the example extension module:

```
git clone https://github.com/saidvandeklundert/c_extension.git
cd c_extension
pip install -e src/
python src/test.py
```